import React from "react";

export const ExportToExcalidrawPlus = () => {
  // Returning null ensures this component renders nothing, effectively removing it
  // from any dialog or menu where it's used.
  return null;
};
